rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * Duniya Verse Security Rules v22.0
     * 
     * CORE PHILOSOPHY:
     * - Explicit 'list' and 'get' separation for query provability.
     * - Unified Whisper Protocol for high-fidelity privacy.
     * - Resilience against missing fields using safe-default lookups.
     */

    function isSignedIn() {
      return request.auth != null;
    }

    function isCommunityAdmin(communityId) {
      return isSignedIn() && request.auth.uid in get(/databases/$(database)/documents/communities/$(communityId)).data.get('admins', []);
    }

    // --- User Profiles & Identity Node ---
    match /users/{userId} {
      allow read: if isSignedIn();
      allow create: if isSignedIn() && request.auth.uid == userId;
      allow update: if isSignedIn() && request.auth.uid == userId;
      
      match /xp_history/{activityId} {
        allow read: if isSignedIn() && request.auth.uid == userId;
        allow create: if isSignedIn() && request.auth.uid == userId;
      }
    }

    // --- Communities & Contexts ---
    match /communities/{communityId} {
      allow read: if isSignedIn();
      allow create: if isSignedIn();
      allow update, delete: if isSignedIn() && resource.data.ownerId == request.auth.uid;

      match /channels/{channelId} {
        allow read: if isSignedIn();
        allow write: if isSignedIn();
        
        match /typing/{userId} {
          allow read, write: if isSignedIn();
        }

        // EXPLICIT MESSAGE PATH - Required for high-fidelity collection listing
        match /messages/{messageId} {
          // 'list' must perfectly match the client-side query filters for provability
          allow list: if isSignedIn() && (
            resource.data.get('visibleTo', ['all']).hasAny(['all', request.auth.uid])
          );
          // 'get' can be broader to allow senders to always see their own nodes
          allow get: if isSignedIn() && (
            resource.data.get('visibleTo', ['all']).hasAny(['all', request.auth.uid]) ||
            resource.data.senderId == request.auth.uid
          );
          allow create: if isSignedIn();
          allow update, delete: if isSignedIn() && (
            resource.data.senderId == request.auth.uid ||
            isCommunityAdmin(communityId)
          );
        }
      }

      match /members/{memberId} {
        allow read: if isSignedIn();
        allow write: if isSignedIn();
      }
    }

    // --- Unified Recursive Message Access (Forwarding & Lineage Trace) ---
    match /{path=**}/messages/{messageId} {
      allow list: if isSignedIn() && (
        resource.data.get('visibleTo', ['all']).hasAny(['all', request.auth.uid])
      );
      allow get: if isSignedIn() && (
        resource.data.get('visibleTo', ['all']).hasAny(['all', request.auth.uid]) ||
        resource.data.senderId == request.auth.uid
      );
      allow create: if isSignedIn();
      allow update, delete: if isSignedIn() && (
        resource.data.senderId == request.auth.uid
      );
    }

    // --- Private Direct Conversations ---
    match /conversations/{conversationId} {
      allow read: if isSignedIn() && (
        !('participantIds' in resource.data) || 
        request.auth.uid in resource.data.participantIds
      );
      allow create, update: if isSignedIn();
    }

    // --- Invitations & Support ---
    match /invitations/{inviteId} {
      allow read: if isSignedIn();
      allow write: if isSignedIn();
    }

    match /contact_form_submissions/{id} {
      allow create: if isSignedIn();
      allow read: if isSignedIn();
    }
  }
}